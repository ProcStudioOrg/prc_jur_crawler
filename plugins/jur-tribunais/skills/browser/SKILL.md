---
name: jur-browser
description: Use when the user asks for jurisprudência, case law, precedents or legal research from any Brazilian court (TJ, TRF, TRT, STF, STJ, TCU) — routes to the right tribunal, refines the query, runs the jur crawler, downloads inteiro teor and produces a structured analysis.
---
<!-- Gerado por `jur/sync-plugin.js`. Edite em `jur/skills/` e rode o sync. -->

# jur-browser — buscar jurisprudência

A skill principal: transforma um pedido do usuário em busca executada, verificada e analisada.

<HARD-GATE>
🚨 NUNCA RODE `jur stj` — O STJ ESTÁ BLOQUEADO DESDE 27/07/2026 (desafio interativo do
Cloudflare: 403 + cf-mitigated: challenge). Diga ao usuário que o STJ está inacessível.
NUNCA cite um REsp de memória para compensar: o `jur-verificador` também não confirma
nada no STJ enquanto durar o bloqueio, então o julgado é NÃO VERIFICÁVEL e não entra.
NÃO EXISTE SUBSTITUTO PARA O STJ em lei federal infraconstitucional — ao oferecer o
TJ/TRF local, rotule explicitamente como instância inferior. Matéria constitucional
segue no `stf`, que está 🟢. Ver o alerta no topo de `CLAUDE.md`.

NUNCA execute a busca sem antes entender a intenção do usuário.
NUNCA cite um julgado sem passar pela skill `jur-verificador`.
NUNCA baixe mais de 50 inteiros teores sem confirmação do usuário.
SEMPRE leia o doc do tribunal (`CLAUDE-<TRIBUNAL>.md`) antes de montar o comando.
SEMPRE informe o usuário a cada refinamento que você fizer.
</HARD-GATE>

## Checklist obrigatório

1. **Entender a intenção** — tema, objetivo (tese favorável / contrária / panorama), recorte temporal.
   Em caso de pedido vago, use a skill `jur-improve-user-prompt` primeiro.
2. **Rotear o tribunal** — tabela em `CLAUDE.md`; cobertura completa em `cobertura/CLAUDE-COBERTURA.md`.
3. **Ler o doc do tribunal** — `CLAUDE-<TRIBUNAL>.md`: flags específicas, operadores que de fato
   funcionam, e as **ressalvas** (é onde mora o que quebra).
4. **Contar antes de coletar** — rodar com `-m 1 --json` para saber o volume.
5. **Refinar** conforme a árvore de decisão abaixo, informando o usuário.
6. **Executar a busca final**, com `--fetch-inteiro-teor` quando a ementa não bastar.
7. **Verificar** — `jur-verificador` sobre a amostra que vai para a resposta.
8. **Analisar e apresentar** no formato da §"Saída".

## Roteamento

| Pedido do usuário | Comando |
|---|---|
| "Tribunal do Paraná", "TJPR", jurisprudência estadual PR | `jur tjpr` (só 2º grau) |
| Juizado Especial / Turma Recursal **estadual** no PR | `jur tjpr --foro juizados` (Justiça Comum é `--foro comum`, o default) |
| "TJGO", Goiás | `jur tjgo` |
| "TJPA", Pará | `jur tjpa` |
| "TJRS", Rio Grande do Sul, estadual RS | `jur tjrs` (só 2º grau) |
| Juizado Especial / Turma Recursal **estadual** no RS | `jur tjrs --origem turmas` (Justiça Comum é `--origem comum`, o default) |
| "TJRJ", Rio de Janeiro, estadual RJ | `jur tjrj` (só e-Proc: Justiça Comum 2º grau, ~2023+; ver `CLAUDE-TJRJ.md`) |
| Jurisprudência **histórica** do RJ (anterior a ~2023), ou acervo carioca antigo | `jur tjrj-ejuris` (módulo legado, base desde ~1995; ver `CLAUDE-TJRJ-EJURIS.md`) — o `jur tjrj` só tem e-Proc ~2023+ |
| Juizado Especial / Turma Recursal **estadual** no RJ | `jur tjrj-ejuris --origem turmas` — ⚠️ acervo **pequeno e só de 2025-2026** (~1,6 mil documentos); diga isso e nunca rotule resultado do `jur tjrj` como Juizado |
| "TJSC", Santa Catarina, estadual SC | `jur tjsc` (⚠️ dois portais no ar — só o comando; ver `CLAUDE-TJSC.md`) |
| Juizado Especial / Turma Recursal **estadual** em SC | `jur tjsc --origem turmas` (Justiça Comum é `--origem comum`, o default) |
| "TJSE", Sergipe, estadual SE, Aracaju | 🔴 **NÃO EXISTE COMANDO — não há nada a rodar.** Os **dois** módulos de jurisprudência estão atrás de captcha (**Turnstile** no judicial, **reCAPTCHA** no administrativo) e este repo não resolve captcha; aqui nem `-n` existe. **Diga isso em vez de entregar zero**, e **nunca cite acórdão do TJSE de memória** (o `verificador` não confirma julgado de SE). Ofereça `trf5` (SE é da 5ª Região), `trt20`/`tst`, `stf` — ⚠️ **nenhum TJ vizinho cobre SE**, não ofereça TJBA nem TJAL. 🟡 pode funcionar no navegador do usuário (Turnstile pune IP de datacenter); ver `CLAUDE-TJSE.md` |
| "TJCE", Ceará, Fortaleza | `jur tjce` (SJURIS: 2º grau + Turma Recursal, **SAJ e PJe juntos**, 814 mil documentos; **sem 1º grau**). Inteiro teor e PDF já vêm na busca. ⚠️ **nunca use `esaj.tjce.jus.br/cjsg`** — cobre só o SAJ e exige browser; ver `CLAUDE-TJCE.md` |
| Juizado Especial / Turma Recursal **estadual** no CE | `jur tjce --base turmas` (Justiça Comum 2º grau é `--base comum`, o default; `--base todos` **mistura os dois**). A ementa da Turma Recursal é 4,5× menor que a do acórdão — baixe o inteiro teor antes de citar tese |
| Decisão monocrática no TJCE | `jur tjce --tipo monocratica --full-text` — ⚠️ **as 247.991 monocráticas vêm com `ementa` VAZIA**: só ACÓRDÃO e TURMA RECURSAL têm ementa indexada. O inteiro teor está no mesmo objeto, então nada se perde — mas sem `--full-text` a saída parece vazia. O crawler emite o aviso em `avisos[]`; repasse |
| Citar julgado do TJCE com link | **NÃO EXISTE PERMALINK** — o SJURIS vive todo em `/tela-consulta`, sem URL por documento. Nunca invente link; verifique por reconsulta `jur tjce -n "<número CNJ formatado>"` |
| "TJDFT", Distrito Federal, Brasília | `jur tjdft` (2º grau + Turma Recursal; **sem 1º grau**; com súmulas e informativos). API pública oficial; inteiro teor já vem na busca. ⚠️ `PROX`/`ADJ` **sem parênteses**; ver `CLAUDE-TJDFT.md` |
| Juizado Especial / Turma Recursal **estadual** no DF | `jur tjdft --acervo turmas` (Justiça Comum 2º grau é `--acervo comum`; o default `--acervo todos` **mistura os dois**). Cada resultado traz `juizado: true/false` |
| Decisão monocrática / da Presidência no TJDFT | `jur tjdft --acervo monocraticas` (ou `presidencia`) — e **filtre por publicação (`-dpi/-dpf`), nunca por julgamento**: esses acervos não têm data de julgamento e `-di/-df` devolve **0 sem erro** |
| "TJMG", Minas Gerais, estadual MG | `jur tjmg` (2º grau + Turmas Recursais; **sem 1º grau e sem súmulas**). ⚠️ operadores em português (`e`/`ou`/`não`/`$`) são **ignorados sem erro** — a sintaxe é `+ \| - "frase" ( ) * ~`; ver `CLAUDE-TJMG.md` |
| Juizado Especial / Turma Recursal **estadual** em MG | `jur tjmg --tipo turmas --escopo inteiroTeor` — **o `--escopo inteiroTeor` é obrigatório**: só `Acórdão` tem ementa indexada, então no escopo padrão o Juizado mineiro devolve **0 sempre**, o que se lê como "não há jurisprudência" e não é. Justiça Comum 2º grau é `--tipo acordao` (o default `--tipo todos` **mistura os dois**). Nesse tipo o campo `relator` vem vazio e a `ementa` é trecho (`ementaEhTrecho: true`) — baixe o inteiro teor antes de citar |
| Filtrar TJMG por comarca / órgão / magistrado / classe / assunto | separe múltiplos valores com **`;`**, não vírgula (18 órgãos julgadores têm vírgula no nome, todos de Turma Recursal). Os nomes precisam ser exatos — o crawler valida contra a API e sugere o correto, porque valor errado devolveria 0 sem erro. Liste com `jur tjmg --listar <campo>` |
| "TJAC", Acre, estadual AC, Rio Branco, Cruzeiro do Sul | `jur tjac` (2º grau + Turmas Recursais, e-SAJ cjsg; **sem 1º grau**, **sem o acervo do e-Proc**, base desde ~2000). A **busca é livre**, mas ver as duas linhas seguintes antes de responder. Ver `CLAUDE-TJAC.md` |
| Juizado Especial / Turma Recursal **estadual** no AC | `jur tjac --origem turmas`. ⚠️ **NO ACRE O JUIZADO É O ACERVO MAIOR — 2,8×** (medido: 2º grau 7.649 × Turmas Recursais 21.353 para o mesmo termo). Isso **inverte** o padrão dos outros TJs: o default `--origem comum` esconde **74%** do acervo em matéria de consumo. Em dano moral / telefonia / banco / transporte aéreo, **rode as duas**. Número baixo em `comum` **não** é escassez de jurisprudência |
| Inteiro teor / acórdão completo do TJAC | 🔴 **não é acessível** — o `getArquivo.do` está atrás de **reCAPTCHA v2** (só o download; a busca é livre, e a sessão da busca não destrava). O que se tem é a **ementa íntegra**, que já vem na busca e é substancial (~4.200 chars em acórdão, ~5.600 em Turma Recursal, no padrão estruturado do CNJ). **Diga que a análise vem da ementa** e não a apresente como o acórdão inteiro |
| Citar / linkar julgado do TJAC | 🔴 **NÃO EXISTE PERMALINK.** Nunca invente link de acórdão do TJAC (o `getArquivo.do` é captcha; o popup de ementa é modal sem URL). A verificação é por **reconsulta**: `jur tjac -n "<nº do processo>"`. Quem identifica o documento é o `cdAcordao`, não o nº do processo |
| Operadores no TJAC | `E`, `OU`, `NAO`, `"frase exata"` funcionam. ⚠️ **`ADJ`, `PROX` e `$` NÃO existem e ZERAM a busca sem erro**. ⚠️ **`NÃO` acentuado não é o operador** de exclusão (vira termo literal: 6.429 × 3.258) — escreva `NAO`. ⚠️ **NÃO avise sobre acento aqui**: ao contrário do TJMS, o índice do TJAC **normaliza** (`usucapiao` e `usucapião` dão 334 os dois) |
| Recorte de data no TJAC | ⚠️ o portal aceita no máximo **1 ano** e devolve **0** acima disso (a tela avisa, mas com HTTP 200 e o formulário de volta) — "o último ano" cai exatamente nisso. O crawler fatia em janelas sozinho e avisa; aí `-m N` vale **por janela** |
| Decisão monocrática do TJAC | ⚠️ acervo **residual** (29 monocráticas contra 7.649 acórdãos para o mesmo termo) — mas **tem ementa**, ao contrário do TJCE. `jur tjac -t monocratica` funciona; não prometa cobertura. E **não existe** a aba "homologação" que o TJMS tem |
| "TJAM", Amazonas, estadual AM, Manaus, Parintins | `jur tjam` (2º grau + Colégios Recursais, e-SAJ cjsg; **sem 1º grau**, **sem o acervo do Projudi**). A busca é livre, mas 🔴 **A BASE CONGELOU EM JAN/2025 — leia a linha seguinte antes de responder**. Ver `CLAUDE-TJAM.md` |
| Jurisprudência **recente** do TJAM (2025, 2026) | 🔴 **A base parou de ser alimentada.** Medido: por data de julgamento, 2024 = 9.023, 2025 = 62, **2026 = 0**; documento mais recente publicado em 06/10/2025. Confirmado em três cortes independentes. **Diga isso ao usuário** — o 0 **não** é ausência de jurisprudência no Amazonas. A base cobre bem **2013–2024**. Matéria federal com origem no AM → `trf1`. **Não há substituto estadual** (Projudi atrás de WAF, PJe não responde, sem e-Proc) |
| Juizado Especial / Turma Recursal **estadual** no AM | `jur tjam --origem turmas`. ⚠️ **NO AMAZONAS O JUIZADO É 7,7× A JUSTIÇA COMUM** (medido: 2º grau 32.755 × Colégios Recursais 252.381 para o mesmo termo) — a inversão do TJAC levada ao extremo. O default `--origem comum` esconde **89%** do acervo em matéria de consumo. Em dano moral / telefonia / banco / plano de saúde / transporte aéreo, **rode as duas**. ⚠️ o filtro se chama "Colégios Recursais", mas o órgão que volta nos dados é `2ª Turma Recursal` |
| Recorte de data no TJAM | ⚠️ **use `-dpi/-dpf` (publicação), NÃO `-di/-df` (julgamento)**: 481 julgados carregam a data-sentinela `01/06/2004`, e numa amostra das 30 publicações mais recentes **11 (37%)** estavam assim — filtrar por julgamento descarta os recentes em silêncio. ⚠️ o portal aceita no máximo **1 ano de calendário** e devolve 0 acima disso (HTTP 200 com o formulário de volta); o crawler fatia sozinho e aí `-m N` vale **por janela** |
| Inteiro teor / acórdão completo do TJAM | 🔴 **não é acessível** — o `getArquivo.do` está atrás de **reCAPTCHA v2** (só o download; a busca é livre, e a sessão da busca não destrava). O que se tem é a **ementa íntegra**, que já vem na busca (média 2.589 chars em acórdão, 1.979 em Colégio Recursal, no padrão estruturado do CNJ). **Diga que a análise vem da ementa** e não a apresente como o acórdão inteiro |
| Citar / linkar julgado do TJAM | 🔴 **NÃO EXISTE PERMALINK.** Nunca invente link de acórdão do TJAM (o `getArquivo.do` é captcha; o `resultadoCompleta.do` em aba limpa devolve o formulário vazio; o popup de ementa é modal sem URL). A verificação é por **reconsulta**: `jur tjam -n "<nº do processo>"`. Quem identifica o documento é o `cdAcordao`, não o nº do processo |
| Operadores no TJAM | `E`, `OU`, `NAO`, `"frase exata"` funcionam. ⚠️ **`ADJ`, `PROX` e `$` NÃO existem e ZERAM a busca sem erro**. ⚠️ **`NÃO` acentuado não é o operador** de exclusão (vira termo literal: 27.719 × 6.574) — escreva `NAO`. ⚠️ **NÃO avise sobre acento aqui**: o índice do TJAM **normaliza** (`usucapiao` e `usucapião` dão 340 os dois) |
| Decisão monocrática / homologação de acordo do TJAM | ⚠️ acervo **residual**: 193 monocráticas contra 472.094 acórdãos, e a aba "homologação" dá **0 em todas as medições**. `jur tjam -t monocratica` funciona e **tem ementa**, mas não prometa cobertura. Diga que **o cjsg não indexa esse tipo em quantidade relevante** — não que o tribunal não decide assim |
| "TJAL", Alagoas, estadual AL, Maceió, Arapiraca | `jur tjal` (2º grau + Colégios Recursais, e-SAJ cjsg; **sem 1º grau**, **sem o acervo do Projudi**, base desde ~2013). Busca livre e ✅ **base CORRENTE** (jul/2026 = 981 publicações; julgado mais recente 23/07/2026) — não repita aqui o alerta do TJAM. ⚠️ o host é `www2`, não `esaj` (`esaj.tjal` é NXDOMAIN). Ver `CLAUDE-TJAL.md` |
| Juizado Especial / Turma Recursal **estadual** em AL | `jur tjal --origem turmas`. ⚠️ **EM ALAGOAS A INVERSÃO SE DESFAZ: a Justiça Comum é 3,3× o Juizado** (medido: 2º grau 103.280 × Colégios Recursais 31.474 para o mesmo termo). **Não generalize a partir de AC (2,8×) e AM (7,7×)** — os três TJs pequenos da família têm relações diferentes. O default `--origem comum` ainda esconde 23% do acervo, então em consumo **rode as duas**. ⚠️ o filtro se chama "Colégios Recursais", mas o órgão que volta é `Turma Recursal Unificada` — e lá o relator vem **genérico** (`Juiz 1 Turma Recursal Unificada`), então `-r` por nome de pessoa não acha nada em Turma Recursal |
| Recorte de data no TJAL | ✅ **os dois pares funcionam** — `-di/-df` (julgamento) e `-dpi/-dpf` (publicação). **Não há data-sentinela** (2004 inteiro = 0; julgamento × publicação batem em 2024: 28.016 × 27.924), ao contrário do TJAM. ⚠️ o portal aceita no máximo **1 ano de calendário** e devolve 0 acima disso (HTTP 200 com o formulário de volta); o crawler fatia sozinho e aí `-m N` vale **por janela** |
| Inteiro teor / acórdão completo do TJAL | 🔴 **não é acessível** — o `getArquivo.do` está atrás de **reCAPTCHA v2** (sitekey própria de AL; só o download, a busca é livre, e a sessão da busca não destrava). O que se tem é a **ementa íntegra**, que já vem na busca e é **a mais rica da família** (média 4.746 chars em acórdão, 3.876 em Turma Recursal, no padrão estruturado do CNJ). **Diga que a análise vem da ementa** e não a apresente como o acórdão inteiro |
| Citar / linkar julgado do TJAL | 🔴 **NÃO EXISTE PERMALINK.** Nunca invente link de acórdão do TJAL (o `getArquivo.do` é captcha; o `resultadoCompleta.do` em aba limpa devolve 200 com **zero cards**; o popup de ementa é modal sem URL). A verificação é por **reconsulta**: `jur tjal -n "<nº do processo>"`. Quem identifica o documento é o `cdAcordao`, não o nº do processo |
| Operadores no TJAL | `E`, `OU`, `NAO`, `"frase exata"` funcionam (prova: `dano` 114.180 = `dano moral` 103.280 + `dano NAO moral` 10.900). ⚠️ **`ADJ` e `PROX` ZERAM a busca sem erro**. ⚠️ **`NÃO` acentuado não é o operador** — escreva `NAO`. ⚠️ **o `$` aqui não zera, DEGENERA**: `dan$` devolve 2, que se lê como "busca específica" e não é. ⚠️ **NÃO avise sobre acento aqui**: o índice **normaliza** (`usucapiao` e `usucapião` dão 1.819 os dois) |
| Decisão monocrática / homologação de acordo do TJAL | ⚠️ a tela **só oferece a aba "Acórdãos"**, mas `jur tjal -t monocratica` **funciona assim mesmo** — acervo residual (43 documentos contra 103.280 acórdãos), com ementa. Diga que **o cjsg não indexa esse tipo em quantidade relevante**, não que o tribunal não decide assim. ⚠️ `-t homologacao` devolve 0 e esse zero é **ambíguo** (o checkbox não existe): **não diga que o TJAL não homologa acordos** |
| "TJBA", Bahia, estadual BA, Salvador, Feira de Santana | `jur tjba` (2º grau + Turmas Recursais, **GraphQL público, sem browser e SEM CAPTCHA nenhum**; cobre **Projudi + PJe**, **sem 1º grau**). ✅ **Base CORRENTE e a maior do bloco estadual**: 4.008.679 documentos, 2026 = 81.737 publicações, documento mais recente 07/08/2026. Ver `CLAUDE-TJBA.md` |
| Juizado Especial / Turma Recursal **estadual** na BA | `jur tjba --origem turmas` (Justiça Comum 2º grau é `--origem comum`, o default; `--origem ambas` mistura). Medido: 1.336 × 835 para o mesmo termo, partição **exata** (soma bate com o total). ⚠️ na **base inteira** a Turma Recursal é 1,6× a Justiça Comum (2.483.821 × 1.524.858), mas **por termo a direção se inverte** — meça, não generalize |
| Operadores no TJBA | 🔴 **NUNCA use `E`, `OU` ou `NÃO`** — o portal oferece os três botões e **os três estão quebrados**: viram palavra literal e **INFLAM** a busca (`usucapião E posse` = **3.596.546** de 4.008.679 documentos, contra 2.171 de `usucapião` sozinho). Os que funcionam são os **ingleses**: `AND` (810), `OR`, `NOT` (1.043), mais `"frase exata"` e curinga `*`. `PROX`/`ADJ` não existem. 🔴 **espaço entre termos é OR, não AND** (prova: 2.171 + 86.140 − 810 = 87.501) — query de duas palavras devolve a **união**. ⚠️ **acento é obrigatório e não normalizado** (`usucapiao` = 4, `usucapião` = 2.171), padrão TJMS e oposto de TJAC/TJAM/TJAL. O crawler avisa nos três casos |
| Contagem / total do TJBA | 🔴 **o total do servidor está INFLADO em `--origem comum`, que é o default**: a API repete cada documento (fator **2,00** — 50 devolvidos, 25 hashes distintos). Com `--origem turmas`/`ambas` o fator é ~1,03. O crawler deduplica por `hash` e publica `totalDeduplicadoEstimado` + `fatorDuplicacao` — **relate o valor deduplicado**, não o `totalResults` |
| Inteiro teor / acórdão completo do TJBA | ✅ **vem de graça na busca — não há captcha em lugar nenhum**, ao contrário de TJAC/TJAM/TJAL. ⚠️ **mas o campo chamado "ementa" É o inteiro teor** (cabeçalho do tribunal, partes, relatório, voto, assinatura): **não existe ementa separada nesta base**. Diga que o texto é o acórdão inteiro; quem quiser só a ementa recorta. `--fetch-inteiro-teor` só grava em disco, sem request extra |
| Citar / linkar julgado do TJBA | 🔴 **NÃO EXISTE PERMALINK.** A URL não muda após a busca (a SPA não tem rota por documento) — nunca invente link de acórdão do TJBA. A verificação é por **reconsulta**: `jur tjba -n "<nº do processo>"`. Quem identifica o documento é o `hash` (UUID), que não é endereçável |
| Filtrar por relator / órgão / classe no TJBA | `-r "<NOME EXATO>"`, `-oj <ids>`, `-c <ids>`; `--listar-filtros` lista os três. 🔴 **assimetria que dá zero calado: `-oj` e `-c` querem o ID, mas `-r` quer o NOME** — passar o id do relator devolve **0 sem erro** (id 185 = 0 × `EMILIO SALOMAO PINTO RESEDA` = 4.435). ⚠️ **não há ordenação por relevância**: `-ord` só aceita `publicacao` (o valor `score` derruba a consulta) |
| Recorte de data no TJBA | ⚠️ **só há filtro de PUBLICAÇÃO** (`-dpi/-dpf`) — `dataJulgamento` vem no resultado mas **não é filtrável**. ✅ **não há teto de intervalo** (5 anos responde normalmente; não precisa fatiar) nem data-sentinela. ⚠️ a curva de **um termo** engana: `usucapião` cai a 0 em 2026 enquanto a base cresce — **meça a base inteira antes de diagnosticar congelamento** |
| Decisão monocrática / acórdão do TJBA | ⚠️ **o filtro `-t` não compõe com `--origem`**: com termo de busca, acórdão + monocrática somam **mais** que o total (`apelação`: 211.538 + 501.375 = 712.913 contra 539.050 reais) e a instância é **ignorada**. Recorte por `--origem`. 📌 e nesta base **Turma Recursal grava como `ACORDAO`, 2º grau como `DECISAO_MONOCRATICA`** — `-t acordao` te dá, na prática, Turma Recursal |
| "TJPB", Paraíba, estadual PB, João Pessoa, Campina Grande, Patos | `jur tjpb` (**1º grau + 2º grau + Turmas Recursais**, API REST pública, sem browser e **SEM CAPTCHA nenhum**; ementa e inteiro teor já vêm na busca, em texto plano). ✅ **Base CORRENTE**: documento do próprio dia (medido em 13/08/2026, 2.515.754 documentos). Ver `CLAUDE-TJPB.md` |
| 1º grau / **sentença** na PB | `jur tjpb --instancia primeiro` — **1.970.661 sentenças, 78% do acervo: o maior 1º grau do repo** (à frente de TJRO, TJES e TJTO). Pedido de sentença paraibana só o TJPB atende. ⚠️ **sentença vem sem ementa e sem relator** (108/108) — o texto dela só existe no inteiro teor |
| Juizado Especial / Turma Recursal **estadual** na PB | `jur tjpb --instancia turmas` (2º grau comum é `--instancia comum`; o default `--instancia todas` **mistura os três**). ✅ A partição **fecha exata**: 8.998 (1º grau) + 3.169 (2º grau) + 41 (Turma Recursal) = 12.208. ⚠️ o peso da Turma Recursal depende do tema (0,3% em `usucapião`) — em consumo, ofereça as duas |
| 🔴 **O PORTÃO do TJPB** (`advanced=true`) | 🔴 **A API tem dois conjuntos de filtros mutuamente exclusivos, e o que está fora do modo ativo é IGNORADO com HTTP 200 e contagem plausível.** Data, comarca, classe, órgão, vara, relator, `instancia` e **número de processo** só valem com `advanced=true`; **`grau` só vale sem ele**. O crawler decide sozinho e avisa quando `--grau` virou recorte de cliente — repasse o aviso. 🔴 **Nunca chame a API do TJPB na mão**: `numeroProcesso` sem o portão devolve a **base inteira** (2.515.754), inclusive para número inventado |
| Operadores no TJPB | ✅ **É o conjunto COERENTE do repo** — português **e** inglês funcionam, com aritmética exata: `E`/`AND` (7.324), `OU`/`OR` (125.729 = 12.206 + 120.847 − 7.324), `NÃO`/`NOT` (4.882 = 12.206 − 7.324), parênteses e `"frase exata"`. **O espaço é `E` (AND)**. Token desconhecido **zera** (sintoma visível). `PROX`/`ADJ`/`PROXIMO` não são operadores. 🔴 **Mas o ACENTO é OBRIGATÓRIO e o índice NÃO normaliza** (`usucapiao` = 64, `usucapião` = 12.208) — padrão TJMS/TJBA. Número baixo aqui é quase sempre acento faltando; o crawler avisa |
| Ementa / inteiro teor do TJPB | 🔴 **Só ACÓRDÃO de 2º grau COMUM tem ementa** (76/76 na amostra de 200). **Acórdão de TURMA RECURSAL não tem** (0/4), nem sentença de 1º grau, nem decisão monocrática — neles o texto é a decisão inteira. Quem tem ementa é o **par (tipo, instância)**, não o tipo. O crawler marca `semEmenta`: **não apresente esse texto como ementa**. ✅ Os dois campos vêm de graça na busca, sem captcha — `--fetch-inteiro-teor` só grava em disco |
| Recorte de data no TJPB | 🔴 **A base só tem data de JULGAMENTO** — `meioPublicacao` veio **null em 200/200**. Não existe publicação; `-dpi/-dpf` são alias que avisam. **Nunca apresente a data do TJPB como publicação.** ⚠️ E ela é um **timestamp de assinatura/indexação** (com milissegundos), não a data da sessão. 🔴 **Meia janela é IGNORADA em silêncio** (só a ponta inicial devolve o acervo inteiro, HTTP 200): mande `-di` **e** `-df`. ✅ `DD/MM/YYYY` cru dá HTTP 400 honesto, não o parse silencioso do TJMT |
| Citar / linkar julgado do TJPB | 🔴 **NÃO EXISTE PERMALINK.** O endpoint de documento (`/public/documentos/{id}?grau=`) responde **404 "Documento vazio"** para o id da busca, e a **tela está atrás do Cloudflare** (403 em todos os assets, inclusive na primeira requisição de um contexto novo) — nunca renderizou. **Nunca invente link do TJPB.** A verificação é por **reconsulta**: `jur tjpb -n "<nº>"`, que aceita **as duas formas** do número. Quem identifica o julgado é o **`id` do documento** — um processo tem vários |
| Filtrar por comarca / classe / órgão / relator no TJPB | `--comarca/--classe/--orgao/--vara/--competencia/--relator` querem **id**, e o id sai do autocomplete: `jur tjpb --listar-filtros comarcas joao`. ⚠️ **os combos são AUTOCOMPLETE** (exigem termo; não há lista canônica) e **o mesmo nome tem vários ids**: três "João Pessoa" (200, 0, 9010) dão 1.689 / 3.169 / 41. ✅ Parâmetro repetido é OR e soma exata; id inventado devolve 0. ⚠️ Mas `grau=9` **não** erra — faz fallback silencioso para `grau=2` |
| Varredura profunda no TJPB | ⚠️ **offset máximo de 10.000** (`max_result_window` do Elasticsearch): acima disso a API devolve **HTTP 404 "Falha ao consultar Elasticsearch"**. O crawler barra antes de gastar request e manda recortar por data. `--page-size` máx. **50** (51+ → HTTP 400 honesto). ✅ Paginação **estável** e total **exato**, sem saturação |
| "TJPE", Pernambuco, estadual PE, Recife, Olinda, Caruaru, Petrolina | `jur tjpe` (2º grau + Turmas Recursais, **API REST pública, sem browser e SEM CAPTCHA nenhum**; cobre **PJe + Projudi**, acervo eletrônico **e** físico, **sem 1º grau**). ✅ **Base CORRENTE**: documento mais recente julgado em 01/08/2026 (medido em 07/08/2026). Ver `CLAUDE-TJPE.md` |
| Juizado Especial / Turma Recursal **estadual** em PE | `jur tjpe --origem turmas` (Justiça Comum 2º grau é `--origem comum`; o default `--origem ambas` **mistura**). ⚠️ **aqui o recorte é feito no CLIENTE**, pelo nome do órgão: o filtro de órgão da API **não é confiável** (o id 7314, de uma Turma Recursal, devolve 3.439 documentos majoritariamente do "1º Grupo de Câmaras Cíveis", e as partições somam 8.090 contra um total de 6.266). **Com `--origem`, o `totalResults` se refere ao acervo SEM o recorte** — o crawler avisa. Quanto pesa depende do tema: `dano moral` = **34%** Turma Recursal, `usucapião` = **0,5%**. Em consumo, ofereça as duas |
| Operadores no TJPE | 🔴 **É o INVERSO do TJBA: aqui os operadores em PORTUGUÊS funcionam e os INGLESES enganam.** ✅ `E` (4.269), `OU`, `NAO` (2.007), **`NÃO` acentuado também** (2.007), `PROX` (13), `"frase exata"` (930). 🔴 `AND` → **0**, `ADJ` → **0**, `OR` → **1**, e **`NOT` → 1.281** — que não zera, devolve número plausível e **diferente do `NAO`**: é a armadilha cara. 🔴 **espaço entre termos é `E` (AND)**: `usucapiao posse` = `usucapiao E posse` = 4.269 < `usucapiao` = 6.266. ⚠️ **`$` é ignorado** (não trunca) e o curinga **`*` devolve MENOS** que o termo inteiro (`usucapi*` = 921). ⚠️ **NÃO avise sobre acento**: o índice **normaliza** (`usucapiao` = `usucapião` = 6.266). O crawler avisa nos casos que importam |
| Contagem / total do TJPE | 🔴 **o total SATURA em 10.000** e a paginação para aí — `recurso`, `posse`, `direito` e `alimentos` batem todos nesse número exato. **Nunca relate 10.000 como número de julgados**; refine com `-di/-df` para contagem exata (o crawler marca `saturado: true` e avisa). ⚠️ a série **por ano** satura em todos os anos de 2019 a 2026 — para medir vigência da base use janela **diária ou mensal** |
| Inteiro teor / acórdão completo do TJPE | ✅ **ementa e inteiro teor são campos DISTINTOS e REAIS, e os dois já vêm na busca**, sem captcha (ementa ≈ 2.4 mil chars, acórdão ≈ 10,7 mil). Diferente do TJBA, onde o campo "ementa" era o inteiro teor. `--fetch-inteiro-teor` só grava em disco, sem request extra. ⚠️ **decisão monocrática vem SEM ementa** (só `textoDecisao`, medido 40/40) — não apresente o texto dela como ementa |
| Citar / linkar julgado do TJPE | 🔴 **NÃO EXISTE PERMALINK — e o link da busca é PIOR que não ter.** A URL que aparece depois de buscar **restaura o formulário mas não executa a busca**: colada numa aba limpa ela mostra **"Nenhum resultado encontrado"** onde existem 6.266 julgados. **Nunca mande esse link ao usuário como prova.** A verificação é por **reconsulta**: `jur tjpe -n "<nº do processo>"`. Quem identifica o documento é a `chave`, não o número do processo (o mesmo processo tem vários julgados) |
| Recorte de data no TJPE | `-di/-df` filtram **julgamento** e restringem de fato (6.266 → 930 em 2025). ✅ **não há teto de intervalo** — não precisa fatiar. ⚠️ **no acervo ELETRÔNICO não existe data de publicação distinta**: `dataJulgamento` e `dataPublicacao` são o **mesmo instante** (medido 60/60, diferem por milissegundos); no acervo **FÍSICO** são datas reais e distintas (60/60). Não prometa recorte por publicação que metade da base não tem |
| Filtrar por relator / classe / assunto no TJPE | `-r "<NOME EXATO>"` (por **nome**, e o nome vem **sem acento** na base), `-c <códigos>`, `-a <códigos>`; `--listar-filtros` despeja 2.076 relatores, 798 classes, 4.457 assuntos e 353 unidades. ⚠️ **`-c` e `-a` estão expostos mas NÃO foram provados por contagem** — pendência declarada em `CLAUDE-TJPE.md` |
| Resultado do TJPE veio com aviso de "documento ilegível" | ⚠️ é real e é da base: existe registro que faz a API responder **HTTP 500 em qualquer página que o contenha** (com `usucapiao`, o offset 186 derruba `page=1&size=100` em 8 de 8 tentativas). O crawler bisecta a página, pula **só** o documento ruim e informa quantos se perderam em `documentosIlegiveis` — **repasse o aviso**, porque o pulado não aparece no resultado |
| `jur tjpe` falhou com erro de TLS / "cadeia incompleta" | ⚠️ **não é bloqueio e não é o portal fora do ar.** O TJPE apresenta só o certificado folha e **omite o intermediário** (`Amazon RSA 2048 M01`); o navegador o busca sozinho pelo AIA, `curl` e Node não — por isso `curl` devolve **HTTP 000**. O `TJPENavigator` embute o intermediário e **mantém a verificação ligada**. Se o erro aparecer, o emissor mudou: atualize a constante, **nunca** desligue a verificação |
| "TJES", Espírito Santo, estadual ES, Vitória, Vila Velha, Serra, Cariacica, Cachoeiro de Itapemirim | `jur tjes` (**API REST pública, sem browser e SEM CAPTCHA nenhum**; ementa e inteiro teor **já vêm na busca**). ✅ **O ÚNICO tribunal do repo com 1º GRAU (sentenças) na base de jurisprudência — e o 1º grau é o MAIOR acervo**: 1.509.942 de 2.212.794 documentos. ✅ Base **CORRENTE** (documento de 07/08/2026, medido em 07/08/2026). Ver `CLAUDE-TJES.md` |
| **Sentença de 1º grau** capixaba / "o que os juízes do ES decidem" | `jur tjes --acervo pje1g` — **é o único tribunal do repo que responde esse pedido**. Nos outros a base é só 2º grau. ⚠️ **O 1º grau NÃO tem ementa**: só o texto integral da sentença |
| Escolher o acervo no TJES | `--acervo`: `pje2g` (default, 2º grau) · `pje1g` (1º grau, 1,5 mi) · `pje2g-mono` (monocráticas) · `fisicos` (acervo físico legado, **tem data de julgamento real**) · `turmas` (Turmas Recursais do Projudi, **só ementa, sem inteiro teor**) |
| Juizado Especial / Turma Recursal **estadual** no ES | `jur tjes --origem turmas` (Justiça Comum é `--origem comum`; o default `--origem ambas` **mistura**). ✅ Aqui a partição **compõe exatamente** (141.155 + 78.488 = 219.643) — diferente do TJPE e do TJBA. ⚠️ Mas o peso depende do tema: `usucapião` = **1,4%** Turma Recursal, `dano AND moral` = **67%** (o dobro da Justiça Comum). Em consumo, ofereça as duas. ⚠️ As Turmas Recursais do **Projudi** são outra base: `--acervo turmas` |
| Operadores no TJES | 🔴 **É o INVERSO do TJPE: aqui os INGLESES funcionam e os PORTUGUESES enganam** (igual ao TJBA). Funcionam `AND` (1.251), `NOT` (323, exato), `OR`, `"frase exata"` (445) e curinga `*`. `E`/`OU`/`ADJ` são **IGNORADOS** (49.789, idêntico ao espaço), e 🔴 `NAO` (52.139) e `PROX` (50.577) **INFLAM** em vez de zerar — inflar não dá sintoma visível. ⚠️ **NÃO avise sobre acento**: o índice normaliza |
| Query de duas palavras no TJES | 🔴 **o espaço é OR, não AND** — provado por aritmética exata (1.574 + 49.466 − 1.251 = 49.789). O resultado é a **união**: o número grande é o segundo termo, não abundância de jurisprudência. **Use `AND`** |
| Recorte de data no TJES | 🔴 **`-di/-df` filtram DATA DE JUNTADA, não julgamento — e a tela do tribunal exibe esse campo rotulado "Julg:"**. Nos três acervos do PJe **não existe** data de julgamento nem de publicação (provado em 2 documentos: "Julg: 15/05/2024" ↔ `dt_juntada 2024-05-15`). **Nunca cite o "Julg:" do TJES como data de julgamento.** Só `--acervo fisicos` e `turmas` têm data real, e a API não filtra por ela |
| Contagem com filtro de data no TJES | 🔴 **um filtro que não exclui nada MUDA a contagem**: `dano moral` = 106.282 e a mesma query de 1900 a 2100 = **61.480** (o conectivo implícito vira AND). Contagem **com** data **não é comparável** com contagem **sem** data — a menos que a query traga `AND`/`OR` explícito. O crawler avisa |
| Citar julgado do TJES | 🔴 **NÃO EXISTE PERMALINK** — a SPA vive toda em `/consulta-jurisprudencia/` e a URL não muda nem ao abrir o documento. Nunca invente link. Verificação por reconsulta: `jur tjes -n "<nº>"`. ⚠️ E **não consulte o CNJ por termo de busca**: devolve 31 documentos, incluindo processos que apenas **citam** o número. ⚠️ O nº do processo **não identifica o julgado** — quem identifica é o `id`, por acervo |
| "TJRR", Roraima, estadual RR, Boa Vista | `jur tjrr` (portal **Juris**, JSF/PrimeFaces, HTTP direto, **sem captcha em etapa nenhuma**; ementa íntegra na busca e **PDF de inteiro teor público**). Base **corrente** (julgado de 07/08/2026), **126.384 documentos**, **só 2º grau + Turma Recursal** — **não tem 1º grau**. Ver `CLAUDE-TJRR.md` |
| Juizado Especial / Turma Recursal **estadual** em RR | `jur tjrr --origem turmas` (Justiça Comum é `--origem comum`; o default `--origem ambas` **mistura**). 🔴 **O peso do Juizado varia 94× conforme o tema**: `dano moral` = **37,5%** (5.965 de 15.907) e `usucapião` = **0,4%** (4 de 991). Em consumo ofereça as duas; em direito real a Turma Recursal é ruído. ✅ A partição por órgão **fecha exata** (as 12 partes somam o total) |
| Tipo de documento no TJRR | `-t todos` (default) · `-t acordao` · `-t monocratica` — são as 2 abas, e ✅ **os dois totais vêm juntos numa requisição só**. 🔴 **Só ACÓRDÃO tem ementa**: a monocrática (49.256, 39% do acervo) vem com card **sem bloco de ementa nenhum** — só processo, relator, órgão e datas, e o texto **só existe no PDF** (`--fetch-inteiro-teor`). O crawler marca `semEmenta`; **não apresente o card da monocrática como ementa**. ⚠️ E o card dela exibe assim mesmo o botão "Copia a ementa" — controle que promete o que o documento não tem |
| Recorte de data no TJRR | ✅ **as duas datas existem, são reais e distintas, e as duas filtram**: `-di/-df` é **julgamento** e `-dpi/-dpf` é **publicação** (diferente de TJPI, TJRO e TJES). 🔴 **A ponta FINAL sozinha é IGNORADA em silêncio** (devolve o acervo inteiro com HTTP 200) enquanto a inicial sozinha funciona — mande as duas pontas; o crawler avisa. ⚠️ O combo diz "TODOS" mas filtra **julgamento**: "TODOS" não é a união dos dois |
| Operadores no TJRR | ✅ **Os PORTUGUESES funcionam, com aritmética exata**: `E`, `OU` (28.908 = 27.442 + 17.373 − 15.907), `NÃO` (11.535 = 27.442 − 15.907), `"frase exata"`, `*` e `$`. **O espaço é E (AND).** 🔴 **Os INGLESES destroem a busca**: `AND` = 4, `OR` = 22, `NOT` = 0, `ADJ` = 4, `PROX` = 1 — viram palavra literal (cair para 4 é sintoma visível, diferente do `NOT` do TJPE). ⚠️ Aqui **`NAO` e `NÃO` são o mesmo operador** (o formulário tira o acento da query inteira) — inédito no repo. ⚠️ **NÃO avise sobre acento**: cliente e índice normalizam |
| Citar julgado do TJRR / conferir número | ✅ **Há permalink por documento e o inteiro teor é PDF público** (`/pdf?id=<id>`, HTTP 200 sem cookie e sem captcha). 🔴 **Quem identifica o julgado é o `id` do portal, não o nº do processo** (o de referência tem **2 acórdãos**). 🔴 **1 em 10 monocráticas não tem PDF nenhum** — o crawler marca `semInteiroTeor`; esse julgado não é baixável. ✅ `jur tjrr -n "<nº>"` aceita **máscara OU 20 dígitos**; `--datajud` confirma o **processo** no CNJ (nunca o julgado). ⚠️ **Não há citação oficial pronta** — monte-a dos campos do card |
| Mandar o link da busca do TJRR como prova | 🔴 **NÃO — não existe URL de busca.** A rota (`/index.xhtml`) nunca muda: o portal é JSF e a busca é POST. Não é o zero falso do TJPE nem o filtro-errado do TJTO; aqui simplesmente não há link a mandar |
| `--page-size` no TJRR | 🔴 **Só 10, 20 ou 30.** Qualquer outro valor devolve a tabela **vazia com HTTP 200** (fragmento de 57 bytes) — `--page-size 50` colheria zero em toda página, o que se lê como fim da lista. O crawler encaixa sozinho no valor válido |
| "TJTO", Tocantins, estadual TO, Palmas, Araguaína, Gurupi | `jur tjto` (portal **Jurisprudência 4.0**, PHP+Solr, HTTP direto, **sem captcha em etapa nenhuma**; ementa íntegra, **citação oficial pronta** e **permalink público que abre o INTEIRO TEOR** já vêm na busca). Base **corrente** (604 acórdãos em ago/2026), 1.102.740 documentos. Ver `CLAUDE-TJTO.md` |
| Juizado Especial / Turma Recursal **estadual** no TO | `jur tjto --origem turmas` (Justiça Comum é `--origem comum`; o default `--origem ambas` **mistura**). ⚠️ **NÃO confunda a competência `TURMAS RECURSAIS` (Juizado, 20.785 acórdãos) com `TURMAS DAS CAMARAS CIVEIS` (2º grau comum, 186.534)** — os dois começam por "TURMAS" e são opostos. Em TO o Juizado é **8,3%** do acervo: padrão TJAL, **oposto** de TJAC/TJAM/TJRO. ⚠️ `--origem comum` é recorte de **cliente** (o portal só tem faceta positiva), então o total do servidor não o reflete. ⚠️ E **não use `--instancia 1` para pegar Juizado**: no TJTO a Turma Recursal conta como 1º grau, então esse filtro mistura TR + monocrática de vara + sentença |
| **Sentença de 1º grau** tocantinense | ✅ `jur tjto -q "<termo>" -t sentenca` — **254.501 sentenças**. É o **4º tribunal do repo com 1º grau**, depois de TJPB, TJRO e TJES; pedido que só esses quatro atendem. ⚠️ **Mas sentença e monocrática só existem de 2024 em diante** (acórdão vai a 2019): pedido histórico devolve pouco e **esse zero não é ausência de decisão** |
| Tipo de documento no TJTO | `-t acordao` (default) | `-t monocratica` | `-t sentenca` — são as 3 abas, e ✅ **os três totais vêm juntos numa requisição só**. 🔴 **Só ACÓRDÃO tem ementa**: em sentença e monocrática o mesmo campo traz **a decisão inteira** (cabeçalho, partes, "SENTENÇA"/"DESPACHO/DECISÃO") e os dois vêm **sem relator** — o crawler marca `semEmenta`, **não apresente esse texto como ementa** e não use `-r` em 1º grau. ⚠️ A aba de monocráticas é a **maior** (597.990) porque **mistura despacho de mero expediente** ("INTIME-SE") com decisão de mérito — não relate esse total como jurisprudência |
| Recorte de data no TJTO | 🔴 **a base só tem data de JULGAMENTO** — o par do documento é (autuação, julgamento) e **não existe publicação**, nem campo nem filtro (espelho do TJRO). Use `-di/-df`. **Nunca apresente a data do TJTO como publicação**, e note que a citação diz "juntado aos autos em …", que é **juntada**. ✅ Meia ponta funciona e o no-op 1900..2100 devolve o total |
| Operadores no TJTO | 🔴 **O ESPAÇO É OR, não AND** — provado por aritmética exata (1.807 + 29.310 − 1.257 = 29.860): query de duas palavras devolve a **união**, e o número grande é o segundo termo. **Use `E` ou `AND`.** 🔴 **`NAO` SEM acento não é operador e INFLA** (30.282 contra 550 da exclusão correta, sem sintoma) — escreva **`NÃO` acentuado** ou `NOT`. **É o oposto do TJAC/TJAM/TJAL** e igual ao TJPI. `ADJ`/`PROX` são **ignorados**; `"frase exata"` funciona; ✅ **`*` e `$` são curinga** (o `$` funcionar é novo no repo). ⚠️ **NÃO avise sobre acento na query** — o índice normaliza; o acento só importa no `NÃO` |
| Citar julgado do TJTO / conferir número | ✅ **EXISTE PERMALINK público** (`documento.php?uuid=`), confirmado em aba limpa, e ele abre o **inteiro teor** (~42 mil chars), não só a ementa. ✅ A **citação oficial vem pronta** (`rodape_ementa`), sem regex. 🔴 **Quem identifica o julgado é o `uuid`, não o nº do processo**: o 0004697-71.2023.8.27.2737 tem **dois** documentos (1 acórdão + 1 sentença). ✅ `jur tjto -n "<nº>"` aceita **máscara OU 20 dígitos** e varre as três abas |
| Mandar o link da busca do TJTO como prova | 🔴 **NÃO.** O permalink de busca existe e **mente sobre o recorte**: por GET o portal ignora **todos** os filtros em silêncio (HTTP 200) e só o termo sobrevive — a URL executa a busca com o filtro errado. Não é o zero falso do TJPE nem o formulário-sem-executar do TJRO; é pior, porque parece certo |
| "TJPI", Piauí, estadual PI, Teresina, Parnaíba, Picos | `jur tjpi` (portal **JusPI**, HTTP direto, **sem captcha em etapa nenhuma**; ementa íntegra, **citação oficial pronta** e **permalink público** já vêm na busca). Base **corrente** (doc. de 31/07/2026), **só 2º grau**, de 2018 em diante. Ver `CLAUDE-TJPI.md` |
| Juizado Especial / Turma Recursal **estadual** no PI | ⚠️ **NÃO EXISTE no TJPI** — os 27 órgãos da base são Câmaras Especializadas, Grupos de Câmaras, Pleno e Vice-Presidências; **não há Turma Recursal indexada**. Não ofereça o recorte e **não leia a ausência como limitação do crawler**. Segundo tribunal do repo assim, depois do TJMT |
| **Súmula do próprio tribunal** / "o TJPI tem súmula sobre isso?" | ✅ `jur tjpi -q "<termo>" -t sumula` — **é o primeiro tribunal do repo com súmulas próprias pesquisáveis** (39). 🔴 Mas **súmula não tem permalink** (HTTP 500 em 5/5) e vem **sem processo, relator, órgão nem citação**: cite pelo texto, nunca por link |
| Recorte de data no TJPI | 🔴 **a base NÃO TEM data de julgamento — só publicação.** Não existe `-di/-df`; use `-dpi/-dpf`. **Nunca apresente a data do TJPI como data de julgamento** (a citação oficial diz "Data DD/MM/YYYY" e é publicação). 🔴 E **uma ponta só é IGNORADA em silêncio**: `-dpi` sozinho devolve o acervo inteiro com HTTP 200 e número plausível (585 = sem filtro). **Mande as duas juntas** — o crawler avisa |
| Operadores no TJPI | 🔴 **são os PORTUGUESES** — `E`, `OU`, `NÃO` **acentuado**, `"frase exata"`, parênteses; a aritmética fecha exata (`OU` = 585 + 27.221 − 306 = 27.500). `AND`/`OR`/`NOT`/`ADJ`/`PROX` **ZERAM a busca**. ⚠️ **`nao` SEM acento não é operador**: vira palavra e a contagem **sobe** (282 contra 279 do `não`), sem sintoma nenhum — **é o oposto do TJAC/TJAM/TJAL**. Espaço = `E` (AND). ⚠️ **NÃO avise sobre acento na query**: o índice normaliza (`usucapiao` = `usucapião` = 585) — o acento só importa para o operador |
| Citar julgado do TJPI / conferir número de processo | ✅ **EXISTE PERMALINK público e estável** (`/jurisprudences/<id>/public`), confirmado em aba limpa — raro no repo. Quem identifica o documento é o **`id`**, não o nº do processo. 🔴 **Mas o número de processo SOZINHO não funciona**, apesar de indexado e de o campo prometer "Processos" — o CNJ **mascarado** derruba a busca com **HTTP 500** e o sem máscara devolve **0 calado**: use `jur tjpi -n "<nº>"`, que aplica o contorno medido. ⚠️ Ele pode arrastar acórdão que apenas **cita** o número — o Checker descarta e avisa |
| Pedido de jurisprudência **recente** do PI (últimos 30–60 dias) | ⚠️ **a indexação está defasada e a curva é visível**: mai/2026 = 10.980, jun = 6.580, jul = 3.782 publicações. A base **está corrente** (doc. de 31/07/2026), mas o período recente ainda está enchendo — **diga isso** em vez de entregar o número baixo como se fosse o acervo. ⚠️ 2025 (37.713) também destoa de 2024 (99.400), sem causa apurada |
| Inteiro teor no TJPI | ✅ Sem captcha: `--fetch-inteiro-teor` faz **1 GET por documento** pelo permalink e traz relatório + voto + decisão (≈18 mil chars em acórdão). ✅ A **ementa íntegra já veio na busca** — o botão "+" do portal não dispara request nenhum. ✅ E **decisão terminativa TEM ementa de verdade** (3.393 chars): **não repita aqui** a ressalva de TJPE/TJCE/TJMT sobre monocrática sem ementa |
| "TJMS", Mato Grosso do Sul, estadual MS, Campo Grande, Dourados | `jur tjms` (2º grau + Turmas Recursais, e-SAJ cjsg, **sem captcha**; **sem 1º grau**). ⚠️ **acento é obrigatório e não é normalizado**: `usucapiao` → 3, `usucapião` → 3.885. Número baixo aqui é quase sempre acento faltando, não ausência de julgado — o crawler avisa, repasse o aviso. Ver `CLAUDE-TJMS.md` |
| Juizado Especial / Turma Recursal **estadual** em MS | `jur tjms --origem turmas` (Justiça Comum 2º grau é `--origem comum`, o default; `--origem ambas` mistura). Medido: 67.328 × 21.801 para o mesmo termo — bases disjuntas |
| Operadores no TJMS | `E`, `OU`, `NÃO`, `"frase exata"` funcionam. ⚠️ **`ADJ` e `PROX` NÃO existem** — viram texto literal e **zeram a busca sem erro**. `NAO` e `NÃO` dão resultados diferentes (acento de novo) |
| Recorte de data no TJMS | ⚠️ o portal aceita no máximo **365 dias corridos** e devolve **0 sem erro** acima disso — "o último ano" cai exatamente nisso. O crawler fatia em janelas sozinho e avisa; nesse caso `-m N` vale **por janela** |
| Jurisprudência **muito recente** do TJMS (jul/2026 em diante) | ⚠️ diga que pode faltar: o TJMS migra para **e-Proc desde 01/07/2026** e o módulo de jurisprudência do e-Proc **não está no ar**. O `cjsg` indexa só o acervo SAJ |
| Decisão monocrática do TJMS | ⚠️ acervo **residual** (48 monocráticas contra 67.529 acórdãos para o mesmo termo). `jur tjms -t monocratica` funciona, mas não prometa cobertura |
| "TJMT", Mato Grosso, estadual MT, Cuiabá, Várzea Grande, Rondonópolis, Sinop | `jur tjmt` (API REST, HTTP direto, **sem captcha em etapa nenhuma**; **ementa, inteiro teor e citação oficial já vêm na busca**). Base **corrente** (indexada em 10/08/2026), **só 2º grau** — 1.543.508 documentos, **sem 1º grau**. Ver `CLAUDE-TJMT.md` |
| Juizado Especial / Turma Recursal **estadual** em MT | 🔴 **NÃO EXISTE nesta base — e a tela do tribunal promete que existe.** Os parâmetros de "Turma Recursal" são **ignorados** pelo servidor e o contador é **0 em toda busca**, inclusive em consumo (`dano moral` = 241.840 acórdãos, **0 recursal**). **Diga ao usuário que o pedido não tem resposta aqui**; esse zero não é ausência de julgado — é o acervo do Projudi não estar indexado. Não ofereça `--origem`: a flag não existe |
| Operadores no TJMT | ✅ `E`, `PROXIMO`, `"frase exata"` e `*` funcionam. 🔴 **`OU` e `NÃO` são DESCARTADOS e a busca vira AND** — você pede união e recebe interseção, com número plausível e **sem sintoma**; para união, rode duas buscas e some. 🔴 **`AND`/`OR`/`NOT` INFLAM 27×** (desligam o AND implícito). `PROX` e `ADJ` **zeram**; `$` **degenera** (`usucapi$` = 2). ⚠️ **não avise sobre acento** — o índice normaliza |
| Recorte de data no TJMT | 🔴 **a janela filtra PUBLICAÇÃO, não julgamento**: use `-dpi/-dpf` (`-di/-df` são alias). A data de julgamento existe no documento e é real, mas **não é filtrável** — **nunca apresente o recorte como sendo por julgamento**. ⚠️ E o filtro do portal está errado para o próprio usuário do site (a API lê MM/DD/YYYY): o crawler converte sozinho, mas nunca chame a API na mão com data brasileira crua |
| Decisão monocrática do TJMT | ⚠️ **vem SEM ementa** (as 454.195 delas) — o que existe é a decisão inteira. O crawler marca `semEmenta` e não a apresenta como ementa; repasse o aviso. `jur tjmt -t monocratica` |
| Citar julgado do TJMT / conferir número de processo | 🔴 **NÃO EXISTE permalink por documento** — nunca invente link de acórdão do TJMT. Quem identifica o documento é o campo `Id`, não o nº do processo (que tem vários julgados). Verificação por reconsulta: `jur tjmt -n "<nº>"`, que **aceita com ou sem máscara**. ✅ A **citação oficial já vem pronta** no resultado. ⚠️ A paginação é **instável**: o crawler deduplica, mas em varredura profunda espere lacunas |
| RJ ou ES federal | `jur trf2` (só 2º grau; base **começa em 2018**). ⚠️ neste portal o **espaço entre termos quebra a busca** — o crawler conserta sozinho, não use `--literal`; ver `CLAUDE-TRF2.md` |
| Juizado Especial **Federal** / Turma Recursal no RJ ou ES | `jur trf2 --origem turmas` (Justiça Federal comum é `--origem trf2`, o default) |
| RS/SC/PR federal, Turmas Recursais previdenciárias | `jur trf4` |
| SP/MS federal | `jur trf3` (⚠️ instável — ver doc) |
| AL/CE/PB/PE/RN/SE federal | `jur trf5` |
| **MG federal, de 2023 em diante** | `jur trf6` (só 2º grau). ⚠️ operadores em **português** e o espaço funciona — **nunca hifenize** a query do TRF6 (é o oposto do TRF2); ver `CLAUDE-TRF6.md` |
| Juizado Especial **Federal** / Turma Recursal em MG | `jur trf6 --origem turmas` (Justiça Federal comum é `--origem trf6`, o default; a origem `varas` existe no site e está **VAZIA**) |
| **MG federal ATÉ 2022** — "jurisprudência antiga de Minas" | `jur trf1`, **não** `trf6`. O TRF6 foi instalado em ago/2022 e sua base começa em **2023**; o acervo mineiro anterior ficou no TRF1 (medido: 27% da amostra do TRF1 em 2019 é de subseções `.4.01.38xx`). Pedido histórico = rodar os dois e dizer que são bases distintas |
| DF/GO/TO/MT/BA/PI/MA/PA/AP/AM/RR/AC/RO federal (e MG até 2022) | `jur trf1` |
| **Matéria constitucional / "o que o Supremo decidiu"** — direitos fundamentais, competência federativa, constitucionalidade de lei, ADI/ADPF/ADC, recurso extraordinário | `jur stf` (base `acordaos`, desde **1892**; instância única) |
| **Súmula vinculante** — "existe súmula sobre isso?", "isso vincula os outros tribunais?" | `jur stf --vinculantes` (são **63**; as 736 simples são `--sumulas-simples`, e `-b sumulas` traz as 799) |
| **Repercussão geral / tema / tese** — precedente que vincula o resto do Judiciário | `jur stf --rg`, `jur stf --tema "<nº ou texto>"`, `jur stf --tese "<texto>"` |
| Desambiguação no STF — **não existe Juizado nem Turma Recursal** | Por **órgão**: `-oj "Tribunal Pleno"` (maior peso) × `-oj "Primeira Turma,Segunda Turma"`. Por **classe**: `-c ADI,ADPF,ADC` (controle concentrado, *erga omnes*) × `-c RE,ARE` (controle difuso) × `-c HC,MS` (originárias) |
| Decisão monocrática de ministro do STF | `jur stf -b decisoes -r "<MINISTRO EM CAIXA ALTA>"` (741 mil docs; é uma **seleção**, não exaustiva) |
| Informativo do STF | `jur stf -b informativos` (⚠️ resumo **sem valor oficial** — nunca cite como se fosse ementa) |
| "Esse processo do STF existe?" / confirmar julgado do Supremo | `jur stf -n "ARE 1596565"` (classe+número, o formato nativo) ou `jur stf -n <CNJ>`. ⚠️ a base **não indexa CNJ**: buscar o número por `-q` devolve zero |
| **Interpretação de LEI FEDERAL infraconstitucional** — Código Civil, CPC, CDC, Código Penal, execução fiscal, benefício previdenciário, contrato, prescrição, "o que o STJ decidiu" | 🔴 **`jur stj` BLOQUEADO desde 27/07/2026 — NÃO RODE.** Desafio interativo do Cloudflare (403 + `cf-mitigated: challenge`); rodar só queima 10 tentativas e devolve `Target page… has been closed`. Diga que o STJ está inacessível; ofereça `trf*`/`tj*` **rotulado como instância inferior**, e diga que a orientação do STJ não pôde ser conferida. Matéria constitucional → `stf` (🟢). **Não cite REsp de memória.** As linhas abaixo valem para quando o bloqueio cair |
| Desambiguação no STJ — **não existe Juizado, Turma Recursal nem 1º grau** | Por **órgão**, e ele define a matéria: `-s publico` (1ª Seção: tributário, administrativo, previdenciário) × `-s privado` (2ª Seção: civil, consumidor, empresarial) × `-s penal` (3ª Seção) × `-oj CE` (Corte Especial). Por **tipo de documento**: `--base acordao` (default) × `--base monocratica` (15× maior, mas não forma jurisprudência colegiada) |
| **Tema repetitivo / precedente qualificado / IAC** — o que vincula os outros tribunais | `jur stj --temas -q "<assunto>"` (tese firmada + questão submetida + situação; roda headless). Os **acórdãos** julgados sob o rito: `jur stj -q "<termo>" --repetitivos` |
| Recorte temático pronto do STJ (superação, distinção, afetação, rol da ANS, insignificância…) | `jur stj -q "<termo>" --nota <chave>` — são 25, escritas pela Secretaria de Jurisprudência do STJ. Liste com `jur stj --listar-notas` |
| Súmula do STJ, Jurisprudência em Teses, Informativo | ⚠️ **não extraídos pelo crawler** (cada um tem tela própria). Diga isso e ofereça as URLs em `CLAUDE-STJ.md` |
| "Esse processo do STJ existe?" / confirmar julgado do STJ | `jur stj -n "REsp 1809043"` (classe+número) ou `jur stj -n "2019/0116080-0"` (registro). ⚠️ a base **não indexa CNJ**: com número CNJ o checker cai no DataJud e confirma só que o **processo** existe, não o julgado |
| Acórdãos de contas, TCU | `jur tcu` |
| **Contas públicas ESTADUAIS/MUNICIPAIS do PARANÁ** — "o que o Tribunal de Contas do PR decidiu", licitação e contrato administrativo, ato de pessoal (admissão, aposentadoria e pensão de servidor), prestação de contas de prefeito/gestor, LRF, subsídio de agente político, terceirização na administração | `jur tcepr` (portal ViaJuris, HTTP direto, **sem captcha**; ementa, **tema**, inteiro teor, tesauro, **citação oficial pronta** e **permalink público** já vêm na busca). ⚠️ **NÃO é Judiciário**: não tem matéria cível/penal/trabalhista, não há número CNJ e não há DataJud. ✅ **O PR não tem TCM** — os 399 municípios estão nesta base. Ver `CLAUDE-TCEPR.md` |
| Súmula / prejulgado / consulta com força normativa do TCE-PR | `jur tcepr -q "<termo>" --classificacao sumula` (ou `prejulgado`, `consulta-forca-normativa`, `consulta-sem-forca-normativa`, `uniformizacao-jurisprudencia`, `incidente-uniformizacao`) |
| "Esse acórdão do TCE-PR existe?" / confirmar julgado do TCE-PR | `jur tcepr -n "393433/2026"` (nº do processo) ou `jur tcepr --acordao "1979/2026"`. 🔴 O número **como a tela mostra** devolve **zero calado** se colado cru no portal — o comando aplica o contorno. ⚠️ Um processo rende **vários** acórdãos: quem identifica o julgado é o `id` do documento |
| **Contas públicas ESTADUAIS/MUNICIPAIS de SANTA CATARINA** — "o que o Tribunal de Contas de SC decidiu", licitação e contrato administrativo, ato de pessoal, prestação de contas de prefeito/gestor, tomada de contas, representação | `jur tcesc` (GraphQL público, HTTP direto, **sem captcha**; **citação oficial pronta** e **PDF público de inteiro teor** já vêm na busca). ⚠️ **NÃO é Judiciário**: sem matéria cível/penal/trabalhista, sem número CNJ e sem DataJud. ✅ **SC não tem TCM** — os 295 municípios estão nesta base. Ver `CLAUDE-TCESC.md` |
| Query de duas palavras no TCE-SC | 🔴 **O espaço é `OR` (união) e NÃO existe `AND`** — e `AND`/`NOT`/`NAO` viram palavra e **INFLAM**, enquanto `E`/`OU`/`OR` são **ignorados**. O único recurso é a **frase exata**: `jur tcesc -q '"merenda escolar"'`. ⚠️ Termo com **menos de 3 caracteres é descartado** e devolve o acervo inteiro (27.783) com HTTP 200 — nunca relate esse total como resultado |
| Ementa / inteiro teor do TCE-SC | 🔴 **A maioria vem SEM ementa**: o texto do card é o **trecho onde o termo casou**, não uma ementa (`semEmenta` + `trechoMatch`). Para o texto integral, `--fetch-inteiro-teor` (PDF público). ⚠️ E a **citação pronta rotula `dataDecisao` como "Sessão"** — não a cite como data de sessão |
| Recorte por data no TCE-SC | 🔴 **Três eixos com coberturas diferentes**: `--eixo-data autuacao` (default, **100%**), `publicacao` (~79%) e `sessao` (**~37%** — descarta 63% em silêncio). Mande `-di` e `-df` |
| Enunciado de consulta / súmula / informativo do TCE-SC | `jur tcesc --base enunciados` (2.564, **força normativa**), `--base informativos` (2.045) ou `--base sumulas`. 🔴 **Escolher a base errada devolve zero que não é ausência de julgado**: `licitação` dá 9.368 em deliberações, 352 em enunciados e **0** nas outras duas. 🔴 **Só há 3 súmulas distintas** no TCE-SC (embutidas no bundle, sem endpoint) — não prometa acervo de súmulas. 🔴 **Enunciado tem vigência** (`vigente`): não cite revogado como orientação atual |
| "Esse acórdão do TCE-SC existe?" / confirmar julgado do TCE-SC | `jur tcesc -n "REP 26/00137305"` (aceita também `2600137305`). ⚠️ Um processo rende **vários** documentos: quem identifica o julgado é o `identificadorDocumento`. 🔴 **Não há URL de busca** — nunca mande o link da busca como prova |
| **Contas públicas ESTADUAIS/MUNICIPAIS do RIO GRANDE DO SUL** — "o que o Tribunal de Contas do RS decidiu", licitação e contrato administrativo, ato de pessoal, contas de prefeito/gestor, tomada de contas, recurso de embargos | `jur tcers` (API REST pública, HTTP direto, **sem captcha**; o **inteiro teor já vem na busca**, conferido contra o PDF). ⚠️ **NÃO é Judiciário**: sem matéria cível/penal/trabalhista, sem número CNJ e sem DataJud. ✅ **O RS não tem TCM** — os municípios gaúchos estão nesta base. Ver `CLAUDE-TCERS.md` |
| Query de duas palavras no TCE-RS | 🔴 **O espaço é `OR` (união)** — provado: 730 + 5.007 − 5.036 = 701 = `AND`. **Use `AND`.** 🔴 E **os operadores que a própria tela anuncia estão todos quebrados**: `E`, `OU`, `NÃO`, `NAO` e `MESMO` **inflam até saturar em 10.000+** (sintoma que se lê como "tema vastíssimo"), `PROX` é ignorado e `$` **zera**. Funcionam `AND`/`OR`/`NOT`/`"frase exata"`. ⚠️ **Não** avise sobre acento, e termo curto **não** é descartado |
| Jurisprudência **recente** do TCE-RS / "me dá a ementa" | 🔴 **A EMENTA DESAPARECE A PARTIR DE 2020** (2019 = 20/20 com ementa; 2020+ = 0/20). Pedido recente volta **sem ementa**, e isso **não é defeito do crawler** — diga ao usuário. ✅ O que existe é o **texto integral** (campo `relatorio`, ~12,7 mil chars), que já vem na busca. 🔴 E **nunca** apresente o campo `texto` como ementa: em 11% ele degenera para uma palavra ("Multa", "Provimento") |
| Contagem do TCE-RS que veio 10.000 | 🔴 **É teto, e o servidor declara** (`total.relacao`). **Não relate 10.000 como contagem** — refine com `-di/-df` ou `--ano`. É o primeiro tribunal do repo em que a saturação vem no payload em vez de ser inferida |
| Recorte por data no TCE-RS | 🔴 **Não existe data de publicação** — o único eixo é a **data da sessão**; `-dpi/-dpf` são alias que avisam. **Nunca apresente a data do TCE-RS como publicação.** ✅ As duas pontas funcionam sozinhas e a janela no-op não altera a contagem |
| "Esse acórdão do TCE-RS existe?" / confirmar julgado do TCE-RS | `jur tcers -n "013714-0200/25-3"` (aceita com ou sem máscara — normaliza). 🔴 **A máscara crua derruba a API com HTTP 500**, não devolve zero. ⚠️ Um processo rende **vários** documentos: quem identifica o julgado é o `id`. 🔴 **Não há URL de busca** — nunca mande o link da busca como prova. 🟢 Mas os **autos inteiros são públicos** (índice de peças por processo), o que nenhum outro tribunal do repo oferece |
| Súmula / parecer / informativo do TCE-RS | `jur tcers --base sumulas` (27), `--base pareceres` (1.195, Auditoria/Consultoria) ou `--base informacoes` (303, Consultoria Técnica). 🔴 **Escolher a base errada devolve zero que não é ausência de julgado** ⚠️ As três tiveram só a contagem medida — campos e card não dissecados |
| **Contas públicas ESTADUAIS/MUNICIPAIS de SÃO PAULO** — "o que o Tribunal de Contas de SP decidiu", licitação e contrato administrativo, ato de pessoal, contas de prefeito/gestor, repasse ao terceiro setor, merenda e transporte escolar | `jur tcesp` (HTTP direto, sem browser, **sem captcha**; 1.317.838 documentos de 2008 em diante, base corrente). ⚠️ **NÃO é Judiciário**: sem matéria cível/penal/trabalhista, sem CNJ e sem DataJud. Ver `CLAUDE-TCESP.md` |
| 🔴 Contas da **capital paulista** / Prefeitura de São Paulo | 🔴 **NÃO ESTÁ NESTA BASE.** O TCE-SP cobre o Estado e os **644 demais municípios**; **São Paulo capital é do TCM-SP**, órgão separado que este repo **não cobre**. **Diga isso** em vez de entregar o número baixo — o zero seria o tribunal errado, não ausência de julgado. (A armadilha do TCM é falsa em PR, SC e RS; **verdadeira em SP**, RJ, BA, GO e PA.) ⚠️ E não há combo de município para conferir |
| Query de duas palavras no TCE-SP | 🔴 **Os operadores são QUATRO CAIXAS, não inline** — primeiro portal do repo assim: `-q` é E (AND), `--frase` frase exata, `--qualquer` OU, `--excluir` NÃO. A aritmética **fecha exata** (17.806 + 89.312 − 16.707 = 90.411 = OR). ⚠️ **Dentro de `-q`, `OU` é DESCARTADO e a busca continua AND** — você pede união e recebe interseção, sem sintoma. `AND`/`OR` inline **zeram**; `NAO` vira palavra. ✅ Aspas e `*` funcionam; **não** avise sobre acento |
| "Me dá a ementa do acórdão do TCE-SP" | 🔴 **NÃO EXISTE EMENTA no TCE-SP, em tipo nenhum** — nem no card, nem no detalhe, nem no PDF. O que há é um **trecho** com o termo destacado (~600–1.200 chars contra ~4.900 do PDF). O crawler marca `semEmenta` em **todos** — **nunca apresente o trecho como ementa nem como acórdão inteiro**. Para o texto, `--fetch-inteiro-teor` (PDF público, sem captcha) |
| Contagem do TCE-SP / "quantos acórdãos há sobre isso" | 🔴 **Um julgado decide VÁRIOS processos e a lista devolve uma linha por processo**: 100 linhas = 84 processos = **35 documentos** (fator 2,86×). "1.699 registros" **não é 1.699 acórdãos** — relate também o `totalDeduplicadoEstimado` (use `-m 5`+, com `-m 1` ele é ruidoso). ✅ O total do servidor é **exato**, não saturado. ⚠️ Página fixa em 10 |
| Recorte por data no TCE-SP | ✅ **O filtro de data mais bem-comportado do repo**: dois eixos reais — `-dpi/-dpf` (publicação) e `-di/-df` (autuação) —, **as duas metades funcionam sozinhas**, a janela no-op não altera a contagem e a aritmética fecha (227 + 1.564 − 92 = 1.699). ⚠️ Só DD/MM/YYYY: ISO devolve **HTTP 400** |
| Súmula ou Boletim de Jurisprudência do TCE-SP | `jur tcesp -t sumula` (52) ou `-t boletim` (50). 🔴 **Família "editorial": TODAS as colunas vêm vazias** — sem processo, sem data, sem parte, sem relator —, e o PDF fica em outro host. Não prometa metadados que não existem. ⚠️ E **"Sentença" no TCE-SP não é 1º grau do Judiciário**: é decisão singular de Conselheiro |
| "Esse acórdão do TCE-SP existe?" / confirmar julgado do TCE-SP | `jur tcesp -n "1681/989/20"` (aceita com ou sem máscara — o Checker formata). 🔴 **Sem a máscara o zero é SILENCIOSO** (o portal reexibe o formulário, sem mensagem). ⚠️ Quem identifica o julgado é o **id do PDF**, não o processo — e o número impresso no PDF **nem é** o da linha que o trouxe. ✅ **Três permalinks públicos**: a **URL da busca** (que de fato executa a busca em aba limpa, ao contrário do TJPE), o `exibir?proc=` e o PDF |
| Relator ou data de publicação de um julgado do TCE-SP | 🔴 **Não vêm na busca** — a tabela tem 8 colunas e nenhuma delas é relator ou publicação. Estão no `exibir?proc=`: use `jur tcesp … --detalhes` (1 GET por processo). ⚠️ E **não há citação oficial pronta** — monte-a dos campos do detalhe |
| **Contencioso administrativo TRIBUTÁRIO federal** — "o que o CARF decidiu", auto de infração da Receita, IRPF/IRPJ/PIS/COFINS/IPI/aduana em recurso administrativo | `jur carf` (API direta; **ementa + inteiro teor já vêm na busca**). É instância ADMINISTRATIVA: a mesma matéria judicializada é `trf*`/`stj`. ⚠️ `OU`/`OR` é **ignorado** (vira E) — para disjunção rode uma busca por termo e some; ver `CLAUDE-CARF.md` |
| Uniformização no CARF / "Câmara Superior" | `jur carf -s "Câmara Superior de Recursos Fiscais"` (a CSRF faz o papel de instância especial; turmas ordinárias = as demais seções) |
| Acervo histórico dos Conselhos de Contribuintes (pré-2009) | `jur carf -s "Primeiro Conselho de Contribuintes"` (idem Segundo/Terceiro — mesma base, 124 mil docs) |
| **Contencioso administrativo PREVIDENCIÁRIO** — "o que o CRPS decidiu", recurso contra indeferimento do INSS, Junta de Recursos, Câmara de Julgamento | 🔴 **NÃO HÁ BUSCA E NÃO HÁ CONTORNO**: o portal exige **login Gov.br** (medido 31/07/2026 — o HTTP 200 é a tela de login; `/api/now/table/*` dá 401), e a tentativa de contornar com perfil de Chrome dedicado **falhou no mesmo dia**: captcha + recusa por navegador desconhecido. **Diga isso ao usuário**; não devolva zero como se fosse ausência de julgado. Ofereça o `trf*` da região para a matéria já judicializada, e os **Enunciados do CRPS** (públicos, PDF no gov.br). Ver `CLAUDE-CRPS.md` |
| "Esse acórdão do CARF existe?" / confirmar julgado do CARF | `jur carf --decisao 2802-000.639` (nº do acórdão) ou `jur carf -n 13890.000160/2006-17` (nº do processo). ⚠️ **só com máscara** (sem pontuação = 0 em silêncio; o Checker formata sozinho). NÃO é CNJ e o DataJud não cobre o CARF |
| SP estadual | ⚠️ `jur tjsp` sem acesso — ofereça TRF3 (ou TRF4/TRF5 como comparativo) |
| "TJMA", Maranhão, estadual MA | ⚠️ **busca por termo não existe** — o JurisConsult exige captcha de imagem + reCAPTCHA e este repo não resolve captcha. **Diga isso ao usuário**, não tente. Ofereça `jur trf1` (o MA é da 1ª Região) para matéria federal; ver `CLAUDE-TJMA.md` |
| Juizado Especial / Turma Recursal **estadual** no MA | Mesmo bloqueio. As flags existem e estão mapeadas (`jur tjma --foro turmas` / `--foro juizados`), mas nenhuma busca completa |
| "Esse processo do TJMA existe?" / confirmar nº de processo do MA | `jur tjma -n <nº CNJ>` — **funciona** (DataJud/CNJ). ⚠️ confirma o **processo**, não o **julgado**: DataJud não tem ementa |
| "TJRN", Rio Grande do Norte, estadual RN, Natal | ⚠️ **busca não existe** — **todo o domínio do TJRN responde 403** (Access Denied do Akamai): o portal de jurisprudência, o site institucional e o `/robots.txt`. **Não é captcha**, não há o que resolver e `--headed` não muda nada. **Diga isso ao usuário**, não devolva zero como se fosse ausência de julgado. Ofereça `jur trf5` (o RN é da 5ª Região) para matéria federal; ver `CLAUDE-TJRN.md` |
| Juizado Especial / Turma Recursal **estadual** no RN | Mesmo bloqueio, e **não há flag**: nenhuma tela do TJRN chegou a carregar. O DataJud mostra que existe acervo (JE 914.799 · TR 189.737), mas ele não é pesquisável daqui |
| "Esse processo do TJRN existe?" / confirmar nº de processo do RN | `jur tjrn -n <nº CNJ>` — **funciona** (DataJud/CNJ, base corrente). ⚠️ confirma o **processo**, não o **julgado**: DataJud não tem ementa. **Nunca cite acórdão do TJRN de memória** — o `verificador` não confirma julgado do RN enquanto o 403 durar |
| **Matéria TRABALHISTA** — verbas rescisórias, horas extras, vínculo de emprego, insalubridade/periculosidade, justa causa, assédio moral, FGTS, adicional noturno | Justiça do Trabalho, **nunca** o TJ do estado (é outro ramo). Escolha pela UF do vínculo: `jur trt9` (PR), `jur trt2` (SP capital), `jur trt15` (SP interior), `jur trt3` (MG), `jur trt1` (RJ), `jur trt4` (RS)… — os 26 comandos em `CLAUDE-FALCAO.md` |
| **Tese trabalhista** — "o que decide a Justiça do Trabalho sobre X", uniformização, súmula/OJ | `jur tst` **primeiro** — o TST uniformiza a CLT para o país inteiro (mesma lógica de "STJ antes do TJ"). Use o TRT para o que é regional |
| ⚠️ Caso trabalhista em **São Paulo** | **SP tem DOIS TRTs**: capital e Grande SP → `jur trt2`; interior (Campinas) → `jur trt15`. Na dúvida rode os dois e diga qual é qual |
| **1º grau trabalhista** — "o que as Varas do Trabalho decidem", sentença | `jur <trt> -g 1` (coleção `sentencas`). ⚠️ **no `tst` não existe** — o comando avisa e manda ao TRT de origem |
| **2º grau trabalhista** — Turmas/Câmaras do TRT, acórdão, recurso ordinário | `jur <trt> -g 2` (default, coleção `acordaos`) |
| Comparar as duas instâncias trabalhistas | `jur <trt> -g ambos` |
| "Juizado Especial trabalhista" / "Turma Recursal trabalhista" | **NÃO EXISTE.** A JT não tem Juizados: o rito sumaríssimo (`-cp ATSum`) é julgado pela mesma Vara, com recurso para as mesmas Turmas. Diga isso e use `-g 1`/`-g 2` |
| Decisão monocrática de desembargador do trabalho | `jur <trt> -g monocraticas` |
| Admissibilidade de Recurso de Revista | `jur <trt> -g admissibilidade` — ⚠️ é ato do **TRT** de origem; o `tst` não tem essa coleção (ele recebe o RR já admitido) |
| Varrer vários TRTs na mesma pergunta | Serialize e espace: os 26 comandos batem no **mesmo host** e rajada rende HTTP 429. Não paralelize — é a exceção à regra geral do repo |

Tribunal não coberto → diga isso explicitamente, mostre o status em
`cobertura/CLAUDE-COBERTURA.md`, e ofereça (a) o tribunal vizinho coberto ou
(b) mapear o tribunal com a skill `jur-codegen`. **Nunca invente resultado.**

Buscas em tribunais diferentes rodam em paralelo — cada crawler sobe seu próprio processo:

```bash
./bin/jur trf4 -q "tempo especial" -di "01/01/2026" -df "31/03/2026" --json -o /tmp/trf4.json &
./bin/jur trf1 -q "tempo especial" -di "01/01/2026" -df "31/03/2026" --json -o /tmp/trf1.json &
wait
```

## Árvore de decisão para refinamento

```
Total da busca inicial (-m 1 --json):
|
|-- <= 50   Busca definitiva. Colete tudo, com --fetch-inteiro-teor se necessário.
|
|-- 51-200  Refine com keywords do tema (tabela abaixo).
|           Informe: "Sua busca retornou N resultados. Refinando com 'KEYWORD'..."
|           Ainda > 50 -> adicione filtro de data (último ano).
|
|-- 201-1000  Keywords + último ano. Ainda > 200 -> últimos 6 meses.
|             Ainda > 200 -> peça recorte ao usuário.
|
|-- > 1000  Refinamento agressivo: keywords + 6 meses.
            Pergunte: "Encontrei N resultados. Posso refinar por X, Y. Quer acrescentar algo?"
            Sem solução -> --max-results 50 e diga que está analisando uma AMOSTRA.
```

## Extração de keywords por tema

| Tema | Keywords para refinar |
|---|---|
| Frentista / posto de combustível | agentes nocivos, hidrocarbonetos, periculosidade, insalubridade, BTEX |
| BPC / LOAS | deficiência, miserabilidade, renda per capita, vulnerabilidade social |
| Auxílio-doença | incapacidade, perícia médica, CID, labor habitual |
| Aposentadoria rural | regime de economia familiar, início de prova material, boia-fria |
| Aposentadoria por idade | carência, tempo de contribuição, idade mínima |
| Tempo especial | agentes nocivos, ruído, PPP, LTCAT, enquadramento por categoria |
| Dano moral consumidor | inscrição indevida, negativação, quantum indenizatório |

### Filtrar por resultado desejado

| Objetivo | Termos adicionais |
|---|---|
| Casos favoráveis | procedente, provimento, reconheceu, deu provimento, acolheu |
| Casos desfavoráveis | improcedente, negou provimento, rejeitou, desprovimento |
| Panorama geral | (não adicionar termos de resultado) |

## Ementa não basta — quando baixar o inteiro teor

- **Turmas Recursais (qualquer tribunal)**: a ementa costuma ser uma frase genérica.
  Inteiro teor é **obrigatório**.
- **TJGO / TJPA**: o texto completo já vem no payload da busca — `--fetch-inteiro-teor`
  grava em disco sem novo acesso à rede. Use sem medo.
- Demais tribunais: baixe quando a análise depender de fundamentação, não só de resultado.

## Operadores

Os operadores válidos **variam por tribunal e por módulo** — estão no `CLAUDE-<TRIBUNAL>.md`.
Não presuma: no TJGO, por exemplo, `E`/`OU`/`NÃO` viram palavra literal no módulo de
jurisprudência (só `"frase exata"` funciona). Sempre cite termos compostos entre aspas:
`-q "aposentadoria especial"`.

No **STF** funcionam todos, em português e minúsculas: `e`, `ou`, `não`, `"frase exata"`,
`"a b"~5` (proximidade), `~` (fuzzy), `$` (curinga), `?` (um caractere) e parênteses.
`ADJ`, `PROX` e `MESMO` **não existem** na pesquisa de jurisprudência do STF (existem no módulo
de Repercussão Geral do portal antigo, que é outra tela). Ver `CLAUDE-STF.md`.

No **STJ** funcionam **todos os oito** da barra do SCON, testados um a um: `e`, `ou`, `não`,
`adj` (e `adj5`), `prox` (e `prox10`), `mesmo` (mesmo campo), `com` (mesmo parágrafo),
`$` (radical) e `"frase exata"`. É a sintaxe mais rica do repositório — use `adj`/`prox` para
precisão. Só `*` não funciona (derruba a consulta). Ver `CLAUDE-STJ.md`.

## Saída

### 1. Resumo quantitativo
Total analisado · favoráveis (n, %) · desfavoráveis · parcialmente procedentes.

### 2. Argumentos recorrentes
As 3-5 teses mais usadas pelos relatores, com frequência.

### 3. Tendência por órgão/turma
Qual turma/câmara tende a ser mais favorável.

### 4. Casos destaque
3-5 acórdãos: nº do processo, relator, trecho-chave **citado do texto retornado**, e por que é relevante.

### 5. Recomendação estratégica
Qual tese priorizar, quais argumentos evitar, qual órgão/relator é mais favorável.

### 6. Verificação
Quantos julgados foram confirmados na base oficial (`jur-verificador`). Diga se algum não confirmou.

## Tabela anti-racionalização

| Pensamento | Realidade |
|---|---|
| "Vou buscar tudo de uma vez" | 6.000 resultados = timeout + contexto estourado. Conte primeiro. |
| "O usuário já disse o que quer" | Confirme: favorável? contrária? panorama? |
| "50 resultados é pouco" | 50 acórdãos inteiros = análise robusta. Comece por aí. |
| "Não preciso informar o refinamento" | O usuário DEVE saber cada refinamento. |
| "A ementa basta" | Em Turmas Recursais a ementa é uma frase. Baixe o inteiro teor. |
| "Posso analisar sem baixar" | Sem inteiro teor você não tem o acórdão. |
| "Vou pular a contagem inicial" | A contagem define a estratégia. NUNCA pule. |
| "Sei as flags de cor" | Leia o `CLAUDE-<TRIBUNAL>.md`. As flags mudam por tribunal. |
| "Esse tribunal deve funcionar" | Confira `cobertura/CLAUDE-COBERTURA.md`. TJSP não roda. |
| "Verifico depois" | Verifique antes de mostrar. Julgado não confirmado não entra na resposta. |
